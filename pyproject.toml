[project]
name = "Flyan"
version = "0.1.5"
description = "Open source unofficial API wrappper to get flight data from RyanAir."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fake-useragent>=1.5.1",
    "httpx>=0.27.2",
    "pydantic>=2.9.2",
    "tenacity>=9.0.0",
]
